# 📁 دليل رفع المشروع - منصة الداعم التعليمي الذكية

## 🎯 المشكلة التي تواجهها:
عندما تختار الملفات يدوياً من المتصفح، لا يتم رفع المجلدات بشكل صحيح.

---

## 🚀 الحل الأول: استخدام GitHub (الأفضل)

### الخطوة 1: إنشاء مستودع على GitHub
1. اذهب إلى [GitHub.com](https://github.com)
2. اضغط على "New repository" (أو الزر الأخضر)
3. اكتب اسم المستودع: `smart-education-platform`
4. اختر "Public" أو "Private"
5. **لا تختر** "Add a README file" (لأن لدينا ملفات بالفعل)
6. اضغط "Create repository"

### الخطوة 2: ربط المشروع المحلي بـ GitHub
```bash
# في مجلد المشروع، شغل هذه الأوامر:
git remote add origin https://github.com/YOUR_USERNAME/smart-education-platform.git
git branch -M main
git push -u origin main
```

**استبدل `YOUR_USERNAME` باسم المستخدم الخاص بك على GitHub**

### الخطوة 3: ربط GitHub بـ Netlify
1. اذهب إلى [Netlify.com](https://netlify.com)
2. اضغط "New site from Git"
3. اختر "GitHub"
4. اختر المستودع `smart-education-platform`
5. استخدم هذه الإعدادات:
   - **Build command:** `npm run build:frontend`
   - **Publish directory:** `dist`
6. اضغط "Deploy site"

---

## 🔄 الحل الثاني: الرفع اليدوي لـ Netlify

### إذا كنت لا تريد استخدام GitHub:

#### الخطوة 1: بناء المشروع محلياً
```bash
npm install
npm run build:frontend
```

#### الخطوة 2: رفع مجلد dist فقط
1. اذهب إلى [Netlify.com](https://netlify.com)
2. اسحب مجلد `dist` (الذي تم إنشاؤه بعد البناء) إلى منطقة النشر
3. انتظر حتى يكتمل الرفع

---

## 📦 الحل الثالث: إنشاء ملف ZIP

### إذا كنت تواجه مشاكل مع Git:

#### الخطوة 1: إنشاء ملف مضغوط
1. اختر جميع الملفات والمجلدات **عدا**:
   - `node_modules/`
   - `dist/`
   - `.git/`
2. اضغط بالزر الأيمن → "Send to" → "Compressed folder"
3. سمي الملف: `smart-education-platform.zip`

#### الخطوة 2: رفع الملف المضغوط
1. اذهب إلى GitHub
2. اضغط "uploading an existing file"
3. ارفع الملف المضغوط
4. GitHub سيفك الضغط تلقائياً

---

## ✅ التحقق من نجاح الرفع

### تأكد من وجود هذه الملفات في المستودع:
```
✅ الملفات الأساسية:
├── package.json
├── package-lock.json
├── vite.config.ts
├── netlify.toml
├── index.html
├── index.tsx
├── App.tsx

✅ المجلدات:
├── src/
├── pages/
├── components/
├── public/
├── prisma/

❌ يجب ألا تظهر:
├── node_modules/
├── dist/
├── .env
```

---

## 🔧 إعدادات Netlify

### عند ربط GitHub بـ Netlify:
- **Repository:** اختر مستودعك
- **Branch to deploy:** `main`
- **Build command:** `npm run build:frontend`
- **Publish directory:** `dist`
- **Node version:** `18` (في Environment variables)

### متغيرات البيئة (اختيارية):
إذا كنت تستخدم API keys:
1. اذهب إلى Site settings → Environment variables
2. أضف المتغيرات المطلوبة

---

## 🚨 مشاكل شائعة وحلولها

### المشكلة: "المجلدات لا ترفع"
**الحل:** استخدم Git أو ملف ZIP، لا ترفع الملفات واحداً واحداً

### المشكلة: "Build failed"
**الحل:** تأكد من وجود `package.json` و `package-lock.json`

### المشكلة: "Page not found"
**الحل:** تأكد من وجود ملف `_redirects` في مجلد `public/`

### المشكلة: "Fonts not loading"
**الحل:** الخطوط محملة من CDN، ستعمل تلقائياً

---

## 📱 اختبار الموقع بعد النشر

بعد النشر الناجح، اختبر:
- ✅ تحميل الصفحة الرئيسية
- ✅ التنقل بين الصفحات
- ✅ تسجيل الدخول (واجهة فقط)
- ✅ الوضع الليلي/النهاري
- ✅ الاستجابة على الهاتف

---

## 🎉 النتيجة النهائية

ستحصل على رابط مثل:
`https://amazing-name-123456.netlify.app`

يمكنك تغيير الاسم من Site settings → Site details → Change site name

---

**💡 نصيحة:** استخدم الحل الأول (GitHub + Netlify) للحصول على تحديثات تلقائية عند تعديل الكود!