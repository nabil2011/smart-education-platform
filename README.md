# منصة الداعم التعليمي الذكي - Backend

منصة تعليمية ذكية شاملة تهدف إلى تحسين تجربة التعلم للطلاب وتسهيل إدارة العملية التعليمية للمعلمين من خلال استخدام التقنيات الحديثة والذكاء الاصطناعي.

## الميزات الرئيسية

- 🔐 **نظام مصادقة آمن** مع JWT وتشفير كلمات المرور
- 👥 **إدارة المستخدمين** (طلاب، معلمين، إداريين)
- 📚 **إدارة المحتوى التعليمي** مع دعم الملفات المتعددة
- 📝 **نظام الاختبارات والتقييم** التفاعلي
- 🎯 **نظام النقاط والتحفيز** مع الشارات
- 🤖 **الذكاء الاصطناعي** لتوليد المحتوى التعليمي
- 🗣️ **تقييم النطق الذكي** للغة العربية
- 📊 **التحليلات والتقارير** المتقدمة
- 🏫 **إدارة المدارس والصفوف**
- 📋 **خطط التعويض والتعزيز**

## التقنيات المستخدمة

- **Backend**: Node.js + TypeScript + Express.js
- **قاعدة البيانات**: MySQL + Prisma ORM
- **التخزين المؤقت**: Redis
- **المصادقة**: JWT + bcrypt
- **الاختبارات**: Jest + Supertest
- **الأمان**: Helmet + CORS + Rate Limiting
- **التوثيق**: OpenAPI/Swagger

## متطلبات التشغيل

- Node.js 18+ 
- MySQL 8.0+
- Redis 6.0+
- npm أو yarn

## التثبيت والتشغيل

### 1. استنساخ المشروع
\`\`\`bash
git clone <repository-url>
cd smart-edu-backend
\`\`\`

### 2. تثبيت التبعيات
\`\`\`bash
npm install
\`\`\`

### 3. إعداد متغيرات البيئة
\`\`\`bash
cp .env.example .env
# قم بتحديث القيم في ملف .env
\`\`\`

### 4. إعداد قاعدة البيانات
\`\`\`bash
# إنشاء قاعدة البيانات
npm run db:generate
npm run db:push
\`\`\`

### 5. تشغيل الخادم
\`\`\`bash
# التطوير
npm run dev

# الإنتاج
npm run build
npm start
\`\`\`

## الاختبارات

\`\`\`bash
# تشغيل جميع الاختبارات
npm test

# تشغيل الاختبارات مع المراقبة
npm run test:watch

# تقرير التغطية
npm test -- --coverage
\`\`\`

## جودة الكود

\`\`\`bash
# فحص الكود
npm run lint

# إصلاح مشاكل الكود
npm run lint:fix

# تنسيق الكود
npm run format
\`\`\`

## واجهات برمجة التطبيقات

### نقاط النهاية الأساسية

- `GET /health` - فحص حالة الخادم
- `POST /api/v1/auth/login` - تسجيل الدخول
- `POST /api/v1/auth/register` - تسجيل مستخدم جديد
- `GET /api/v1/users/profile` - الملف الشخصي
- `GET /api/v1/content` - المحتوى التعليمي
- `POST /api/v1/assessments` - إنشاء اختبار

### المصادقة

جميع واجهات برمجة التطبيقات المحمية تتطلب رأس Authorization:
\`\`\`
Authorization: Bearer <jwt-token>
\`\`\`

## بنية المشروع

\`\`\`
src/
├── controllers/     # تحكم في المنطق
├── middleware/      # الوسطاء
├── routes/         # مسارات API
├── services/       # منطق العمل
├── utils/          # أدوات مساعدة
├── types/          # تعريفات TypeScript
├── __tests__/      # الاختبارات
└── index.ts        # نقطة البداية
\`\`\`

## المساهمة

1. Fork المشروع
2. إنشاء فرع للميزة الجديدة (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push للفرع (`git push origin feature/amazing-feature`)
5. فتح Pull Request

## الترخيص

هذا المشروع مرخص تحت رخصة MIT - راجع ملف [LICENSE](LICENSE) للتفاصيل.

## الدعم

للحصول على الدعم، يرجى فتح issue في GitHub أو التواصل مع فريق التطوير.